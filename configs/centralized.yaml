# Centralized baseline: ImageNet initialization, full train split.
base: base.yaml
name: centralized
seed: 0

centralized:
  epochs: 20
  optimizer: adam
  lr: 0.001
  lr_step_size: 7
  lr_gamma: 0.1
